/**
 * Absolute origin of the deployed site. Needed so OpenGraph image paths
 * resolve — Next can't infer it during a static export.
 */
export const SITE_URL = new URL("https://yuvalelarat.github.io");
